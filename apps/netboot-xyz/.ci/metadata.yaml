app: netboot-xyz

# renovate: datasource=github-releases depName=netbootxyz/netboot.xyz
version: 2.0.86

builds:
  - name: main
    platforms:
      - linux/amd64
    args:
      BOOT_DOMAIN: netboot.hemul.net